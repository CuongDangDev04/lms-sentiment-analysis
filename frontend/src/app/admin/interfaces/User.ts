export interface User {
    id: number;
    username: string;
    password: string;
    fullname: string;
    role: string;
    email: string;
    avt?: string;  
    birthdate?: string;  
    phone?: string;  
}
